<h1><%= t('.title') %></h1>

<%= render 'form', movie: @movie %>

<div class="ui buttons">
  <%= link_to t('.back'), movies_path, class: 'ui button' %>
  <div class="or"></div>
  <%= link_to t('.show'), @movie, class: 'ui button teal' %>
</div>